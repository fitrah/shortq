import Link from 'next/link';

export function Input({ name, type, label, minLength }: { name: string; type: string; label: string; minLength?: number }) { return <label className="block text-sm text-slate-300">{label}<input required name={name} type={type} minLength={minLength} className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 p-3 outline-none focus:border-cyan-400" /></label>; }
export function AuthShell({ title, children, footer }: { title: string; children: React.ReactNode; footer: React.ReactNode }) { return <main className="flex min-h-screen items-center justify-center bg-slate-950 px-5 text-white"><section className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900 p-8"><Link href="/" className="mb-8 block font-black"><span className="text-cyan-400">go.</span>proyek.org</Link><h1 className="mb-6 text-3xl font-black">{title}</h1>{children}<p className="mt-6 text-center text-sm text-slate-400">{footer}</p></section></main>; }
